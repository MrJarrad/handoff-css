# handoff-css

Turn a Figma **Design Handoff** export into production-ready CSS.

Scaffolding in progress — see `presets/jhd.config.mjs` for the worked config and
`test/parity.test.mjs` for the guarantee this package is built against.
